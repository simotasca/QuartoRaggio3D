import anim from '../styles/animation.module.scss';

const PAGE_TOGGLE_TIME_MS = parseInt(anim.pageToggleDurationMs);
const MACRO_CHANGE_TIME_MS = parseInt(anim.macroChangeDurationMs);
const WAIT_BETWEEN_MS = 200;

export { PAGE_TOGGLE_TIME_MS, MACRO_CHANGE_TIME_MS, WAIT_BETWEEN_MS }